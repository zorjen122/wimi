# 主任务清单

## P0：消息正确性与群聊基础能力

- [ ] 客户端消息排序与有序性：以 `conversation_id + conversation_seq` 为准处理实时推送、补拉、重复包和乱序包；保证本地持久化、UI 展示及同步游标一致。
- [x] 文本消息已读通知推送：接收者 `READ ACK` 持久化后，向发送者实时推送 read receipt；发送者离线时可通过会话同步补偿。
- [ ] 文件上传与下发：完成上传、对象/文件存储、文件元数据持久化、权限校验、下载/下发和失败重试；将文件消息纳入会话序列、同步与 ACK 语义。
- [ ] 群聊支持：实现群主、管理员、普通成员的角色及权限管理；完成群文本消息、申请加群、审批、拒绝、退出群等基本能力。

## P0：需与群聊一起完成的正确性修复

- [ ] 修复群审批：拒绝不得写入成员关系；审批前必须校验操作者是该群群主或管理员；操作需要幂等。
- [ ] 补齐群申请的查询/同步：`groupApplys` 必须有正确的拉取接口和客户端接入，不能依赖仅在线时的实时通知。
- [ ] 明确并实现 ClientForward 结果处理：Message 端消费 `ClientForwardAck`，区分已排队、离线、过期路由和背压；对可持久化业务走同步补偿，对通知定义持久化或丢弃策略。

## P1：连接与可观测性

- [ ] 完善 Client-Gateway 心跳失活检测：客户端记录最后一次 `PING_RSP`，连续超时后主动断开并复用现有重连流程。
- [ ] 为重连、重复转发、乱序同步、群角色权限、群审批和已读回执增加端到端测试。
